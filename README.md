# Tech Service REST API 🛠️

Backend-приложение для управления сервисом технических услуг. Проект реализует полный цикл работы: от ведения каталога услуг и регистрации пользователей до бронирования, управления ролями, финансовой отчетности и интеграции с внешними системами. 

![Java](https://img.shields.io/badge/Java-23-orange)
![Spring Boot](https://img.shields.io/badge/Spring_Boot-4.0.1-green)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-blue)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED)

## 📋 Содержание
- [Технологический стек](#-технологический-стек)
- [Запуск проекта](#-запуск-проекта)
- [Документация API](#-документация-api)
- [Реализованный функционал](#-реализованный-функционал)
- [Настройки и Профили](#-настройки-и-профили)

## 🛠 Технологический стек

* **Core:** Java 23, Spring Boot 4 (Web, Data JPA, Validation, Security, Mail, Scheduling)
* **Database:** PostgreSQL
* **Auth:** Spring Security + JWT (Access/Refresh tokens)
* **API Docs:** SpringDoc OpenAPI (Swagger UI)
* **Integration:** OpenFeign, RestClient
* **Tools:** Docker, Docker Compose, Maven, Lombok

## 🚀 Запуск проекта

Для запуска требуется установленный **Docker** и **Docker Compose**.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/your-username/tech-service.git
    cd tech-service
    ```

2.  **Настройте переменные окружения (опционально):**
    Проверьте файл `docker-compose.yml` или создайте `.env` файл для секретов (пароли БД, почты).

3.  **Запустите контейнеры:**
    Команда соберет JAR-файл и поднимет базу данных и приложение.
    ```bash
    docker compose up -d --build
    ```

4.  **Проверка статуса:**
    Приложение будет доступно по адресу `http://localhost:8080`.

## 📚 Документация API

В проекте настроен **Swagger UI** для удобного тестирования эндпоинтов.

* **Swagger UI:** [http://localhost:8080/swagger-ui/index.html](http://localhost:8080/swagger-ui/index.html)
* **OpenAPI JSON:** [http://localhost:8080/v3/api-docs](http://localhost:8080/v3/api-docs)

> Вы можете импортировать JSON-спецификацию в **Postman** для автоматического создания коллекции запросов.

## ✅ Реализованный функционал

Проект разработан в рамках серии практических заданий и включает следующие модули:

### 1. Управление услугами (Catalog)
* CRUD операции для технических услуг.
* Создание, редактирование, получение списка и просмотр деталей услуги по ID.

### 2. Бронирование (Booking System)
* Создание клиентом заявки на услугу.
* Валидация входящих данных (корректность времени, наличие услуги).
* Возможность переноса времени записи (Reschedule).
* Отмена бронирования (клиентом или администратором).
* Просмотр истории бронирований и списка оказанных услуг.

### 3. Аналитика и Поиск
* Поиск бронирований по дате и времени.
* **Финансовый отчет:** Получение данных о выручке за определенный период (группировка по датам).
* Глобальная обработка ошибок через `@ControllerAdvice` и кастомные исключения.

### 4. Ролевая модель и Администрирование
В системе реализованы роли: `USER`, `OPERATOR`, `ADMIN`.
* **Администратор:**
    * Управление профилями операторов.
    * Редактирование бронирований любых пользователей.
    * Установка персональных скидок на конкретную запись.
* **Пользователь:** Доступ только к своим данным и публичному каталогу.

### 5. Безопасность (Security)
* Аутентификация на базе **JWT** (Stateless Session).
* Разграничение доступа к эндпоинтам через `requestMatchers` и аннотации `@PreAuthorize` / `@Secured`.

### 6. Система уведомлений (Notifications)
Асинхронная отправка Email-уведомлений:
* Подтверждение создания, изменения или отмены брони.
* **Программа лояльности:** Автоматическая отправка письма с извинениями и промокодом на скидку, если бронь была отменена по инициативе сервиса (Админом/Оператором).
* Персонализированные обращения по имени.

### 7. Интеграция и Scheduled Tasks
Реализована выгрузка статистики в DWH (Data Warehouse) раз в сутки:
* Сбор данных о предоставленных услугах (со статусом `COMPLETED`).
* Отправка HTTP POST запросов во внешнюю систему.
* **Две реализации клиента** (переключаются через профили Spring):
    1.  **OpenFeign:** С настроенным `Retryer` (повторные попытки при сбоях) и `ErrorDecoder`.
    2.  **RestClient:** Реализация на базе `Spring WebClient / RestClient`.
* Механизм повторной отправки данных в случае неудачи.

## ⚙️ Настройки и Профили

Приложение поддерживает переключение реализаций HTTP-клиентов через профили Spring.

В `docker-compose.yml` или `application.properties`:

* `SPRING_PROFILES_ACTIVE=feign` — использовать OpenFeign для интеграции с DWH.
* `SPRING_PROFILES_ACTIVE=rest` — использовать RestClient.
* `SPRING_PROFILES_ACTIVE=dev` — включение заглушек (например, Mock Mail Service) для локальной разработки.
* `SPRING_PROFILES_ACTIVE=prod` — реальный сервис EmailSender для передачи уведомлений пользователям.

---
